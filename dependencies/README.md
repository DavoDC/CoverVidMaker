# Dependencies

## FFmpeg

Download from https://github.com/GyanD/codexffmpeg/releases and place `ffmpeg.exe`, `ffprobe.exe`, and `ffplay.exe` in `dependencies/ffmpeg/`.

The binaries are gitignored - each developer downloads them once.
